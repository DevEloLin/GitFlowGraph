node_modules/
target/
dist/
*.js
*.d.ts
tsconfig.tsbuildinfo

# Static site assets — committed JS that ships to GitHub Pages.
# Without this negation, the broad `*.js` rule above silently swallows
# `docs/assets/i18n.js` and the deployed site 404s on the i18n runtime.
!docs/assets/*.js
